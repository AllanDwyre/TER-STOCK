import 'package:flutter/material.dart';

import 'app.dart';

// https://pub.dev/packages/formz is for form validation : simplify form representation and validation in a generic way
void main() {
  // Bloc.observer = const AppBlocObserver(); // ?https://pub.dev/packages/flutter_bloc/example
  runApp(const App());
}
